@page
@model TwoFactorAuthenticationModel
@{
    ViewData["Title"] = "Two-factor authentication (2FA)";
    ViewData["ActivePage"] = ManageNavPages.TwoFactorAuthentication;
}

<partial name="_StatusMessage" for="StatusMessage" />
<h5><i class="fas fa-user-lock"></i>&nbsp;Konto sikkerhed</h5>

@if (Model.Is2faEnabled)
{
    if (Model.RecoveryCodesLeft == 0)
    {
        <div class="alert alert-danger">
            <strong>You have no recovery codes left.</strong>
            <p>You must <a asp-page="./GenerateRecoveryCodes">generate a new set of recovery codes</a> before you can log in with a recovery code.</p>
        </div>
    }
    else if (Model.RecoveryCodesLeft == 1)
    {
        <div class="alert alert-danger">
            <strong>You have 1 recovery code left.</strong>
            <p>You can <a asp-page="./GenerateRecoveryCodes">generate a new set of recovery codes</a>.</p>
        </div>
    }
    else if (Model.RecoveryCodesLeft <= 3)
    {
        <div class="alert alert-warning">
            <strong>You have @Model.RecoveryCodesLeft recovery codes left.</strong>
            <p>You should <a asp-page="./GenerateRecoveryCodes">generate a new set of recovery codes</a>.</p>
        </div>
    }

    if (Model.IsMachineRemembered)
    {
        <form method="post" style="display: inline-block">
            <button type="submit" class="btn btn-outline-info btn-sm">Glem mit login i denne browser.</button>
        </form>
    }
    <a asp-page="./Disable2fa" class="btn btn-outline-info btn-sm">Fjern 2FA</a>
    <a asp-page="./GenerateRecoveryCodes" class="btn btn-outline-info btn-sm">Nulstil gendannelseskoder</a>
}


@if (!Model.HasAuthenticator)
{
    <h5 class="text-info small">Authenticator app</h5>
    
    <p class="small text-muted">
        For at sikre din konto, kan du tilknytte en Authneticator app.
        Det betyder at du, når du logger ind, skal angive en kode fra din Authenticator App.<br />
        På den måde kan du sikre dig at andre ikke får adgang til din konto
    </p>

    //<p class="small text-muted">Hvis du ikke har brugt en Authenticator App før, så kan du kigge på Microsoft Authenticator</p>

    <a id="enable-authenticator" asp-page="./EnableAuthenticator" class="btn btn-outline-info btn-sm">Tilføj App</a>
}
else
{
    <a id="enable-authenticator" asp-page="./EnableAuthenticator" class="btn btn-outline-info btn-sm">Indstil app</a>
    <a id="reset-authenticator" asp-page="./ResetAuthenticator" class="btn btn-outline-info btn-sm">Nulstil app</a>
}

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}