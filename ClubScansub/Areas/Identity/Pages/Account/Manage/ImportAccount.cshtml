@page
@model ImportAccountModel
@{
}

@{
    ViewData["Title"] = "Importer turkonto fra gammelt system";
    ViewData["ActivePage"] = ManageNavPages.ImportOldAccount;
}

<h5 class="text-black"><i class="fas fa-key"></i>&nbsp;Importer gammel turkonto</h5>

<div class="row">

    @if (Model.Input.AlreadyImported)
    {
        <h3>Du har allerede importeret din gamle konto</h3>
        <p>Din konto er allerede imporertet en gang og kan ikke importeres igen.</p>
        <p>Se dine gamle kontooplysninger under 'turkonto'</p>
    }
    @*else
    {


        <div class="col-md-6 col-12">
            <form id="import-oldaccount-form" method="post">
                <div asp-validation-summary="All" class="text-danger"></div>
                <div class="form-group small">
                    <label asp-for="Input.Usernumber"></label>
                    <input asp-for="Input.Usernumber" class="form-control form-control-sm" />
                    <span asp-validation-for="Input.Usernumber" class="text-danger"></span>
                </div>
                <div class="form-group small">
                    <label asp-for="Input.Password"></label>
                    <input asp-for="Input.Password" class="form-control form-control-sm" />
                    <span asp-validation-for="Input.Password" class="text-danger"></span>
                </div>
                <button type="submit" class="col-auto btn btn-outline-success btn-sm"><i class="fas fa-save"></i>&nbsp;Importer gammel turkonto</button>
            </form>
        </div>
    }*@
</div>
    

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}